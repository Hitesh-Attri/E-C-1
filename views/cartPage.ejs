<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cart Page</title>
    <link href="/style/home.css" rel="stylesheet" />
    <link href="/css/bootstrap.min.css" rel="stylesheet" />
  </head>

  <body>
    <%- include('../views/partials/header', {username: username, loggedIn: loggedIn, isCart:false, isAdmin: isAdmin}); %>

    <main>
      <!-- <h3>Cart Page ---=></h3> -->

      <% let x=products.length %>
      <div class="container">
        <div class="row">
          <div class="col-md-12">
            <% if(x==0){ %>
              <h2>Cart empty!</h2>
            <% }else{ %>
              <h2>Product-List</h2>
            <% } %>
            <div class="row" id="parDiv">
              <% for(let i=0; i < x; i++){ %>
                <div class="col-md-3">
                  <div class="product" style="background: #fff; height: 22rem;">
                    <div class="text-center" style="height: 13rem;">
                      <img src="/<%= products[i].file_name %>" style="height: 100%; width: 80%;" />
                    </div>
                    <p><%= products[i].product_name %></p>
                    
                    <% if(products[i].isAvailable){ %>
                      <div class="my-2 text center d-flex" id="<%= products[i].product_id %>">
                        <a href="/product/details/<%= products[i].product_id %>" class="btn btn-secondary btn-sm mx-2">View details</a>
    
                        <button id="<%= products[i].product_id %>" class="btn btn-secondary btn-sm addToCartBtn mx-2 minus" >-</button>
                        <p id="p<%= products[i].product_id %>"><%= products[i].quantity %></p>
                        <button id="<%= products[i].product_id %>" class="btn btn-secondary btn-sm addToCartBtn mx-2 plus"  >+</button>
                        <button id="<%= products[i].product_id %>" class="btn btn-secondary btn-sm addToCartBtn mx-2 deleteBtn">&#128465;</button>
          
                      </div> 
                    <% }else{ %>
                      <h3>Currently unavailable!</h3>
                      <button id="<%= products[i].product_id %>" class="btn btn-secondary btn-sm addToCartBtn mx-2 deleteBtn">&#128465;</button>
                    <% } %>
                    <% if(products[i].isAvailable && false){ %>
                      <p>stock: <%= products[i].stock %></p>
                    <% } %>
                    </div>
                    <br/>
                  </div>
              <% } %>
            </div>
          </div>
        </div>
      </div>
      <br>
      
      <script src="/script/cartScript.js"></script>
    </main>
  </body>
</html>
